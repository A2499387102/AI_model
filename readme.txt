需求背景：开发一套支持二分类。回归连续值的机器学习工具
数据分布及二分类标签分箱数据
1.计算特征本身的分布，描述性分析，缺失率，一值率，分位数（数值型），类别型排除在外不做处理，以及可以按照基准组对照组计算对应的psi，以及特征之间的相关性
2.根据二分类标签，0，1去计算特征分箱，分箱暂时用等频10分箱，并且可以直接操作特征为对应的10分箱的特征处理，然后使用分箱处理后的特征进行建模
3.根据上一步骤分箱的逻辑计算特征的iv值
根据以上数据进行特征的筛选，iv默认为0.001，psi默认为0.5，缺失率默认为0.98，相关性默认为0.97，然后输出全量特征的数据分布数据及psi，iv，相关性数据的excel报告
模型训练
4.训练主要使用两种，lightgbm和xgboost，训练的框架你可以读取当前路径下的lgbm-Copy1和xgboost代码_new_权重的ipy文件，你能判断出中间的训练部分代码的话，取这个训练逻辑，不确认就跟我交互
5.输出二分类模型评估报告和回归模型的模型评估报告


第二轮需求：
1.模型训练本身需要在jupyter页面中交互进行，会考虑更改代码包中的参数空间的一些参数设置，如何不重启，修改参数继续完成训练，给出你的方案
2.完善一个功能，模型训练后在当前路径下保存模型文件，入模特征文件以及jupyter页面可以按照某列值如月份或者dataset（区分train，test，oot）输出ks，auc情况

第三轮需求：
1.回归模型的模型评估报告指标添加MAPE指标，报告内容除了样本量，给每一箱的占比，并且添加根据月份列来展示按月的分箱指标
2.分类模型的模型评估报告指标添加lift，报告内容除了样本量，给每一箱的占比，并且添加根据月份列来展示按月的ks，以及按月的分箱指标
3.特征输出报告中，增加二分类下，可以根据dataset列(train,test,oot区别)来计算每一块样本集的特征的分箱图（每一箱包含坏样本占比，及该箱的样本占比）