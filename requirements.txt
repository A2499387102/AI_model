# 核心依赖（ml_tool 实际使用；版本为当前已验证环境，>= 表示最低可用版本）
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
openpyxl>=3.1
joblib>=1.3
lightgbm>=3.3
xgboost>=2.0
hyperopt>=0.2.7
tqdm>=4.66

# 可选：旧参考 notebook（lgbm-Copy1.ipynb / xgboost代码_new_权重.ipynb）与文档中的
# GLM 评分标准化 / 特征解释工作流需要。当前 ml_tool 已自行实现 WOE/IV，未直接依赖，
# 如需运行旧 notebook 或做评分卡 GLM 缩放 / SHAP 解释，请取消注释：
# statsmodels>=0.14
# toad>=0.1.2
# shap>=0.44
